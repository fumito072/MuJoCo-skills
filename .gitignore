# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/

# MuJoCo runtime log
MUJOCO_LOG.TXT

# macOS
.DS_Store

# Editor
.vscode/
.idea/

# Robot models — fetched by mujoco-env-setup (kept out of git to keep `npx skills add` fast)
models/

# VLA experiment venv (heavy, not committed)
.venv-vla/

# LLM/voice/sim venv (mlx-lm + whisper + mujoco + torch; heavy, not committed)
.venv-llm/

# RL/Playground exploration venv (jax/brax; heavy)
.venv-rl/
runs_climb/
